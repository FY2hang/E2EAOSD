backbone:
  # [from, module, args]
  - [-1, StemBlock, [16, 16, True]] # 0-P2/4
  - [-1, HG_Stage, [16, 64, 1, 3, False, False, 3, True, 'se']] # 1-P2/4
  - [-1, HG_Stage, [32, 256, 1, 3, True, False, 3, True, 'se']] # 2-P3/8
  - [-1, HG_Stage, [64, 512, 2, 3, True, True, 5, True, 'se']]  # 3-P4/16
  # - [-1, HG_Stage, [128, 1024, 1, 3, True, True, 5, True, 'se']] # 4-P5/32

encoder:
  - [1, ConvNormLayer_fuse, [256, 1, 1]] # 4-P2/4
  - [2, ConvNormLayer_fuse, [256, 1, 1]] # 5-P3/8
  - [3, ConvNormLayer_fuse, [256, 1, 1]] # 6-P4/16

  - [-1, TransformerEncoderBlock, [8, 1024, 0.1, "relu", 10000, False]] # 7-P4/16

  - [-1, ConvNormLayer_fuse, [256, 1, 1]] # 8
  - [-1, nn.Upsample, [None, 2, "nearest"]] # 9
  - [[-1, 5], Concat, []] # 10-P3/8
  # - [-1, RepNCSPELAN4, [256, 512, 64, 1, False, 'silu']] # 11-P3/8
  - [-1, Faster_Block, [256]]

  # - [-1, ConvNormLayer_fuse, [256, 1, 1]] # 12
  # - [-1, nn.Upsample, [None, 2, "nearest"]] # 13
  # - [[-1, 4], Concat, []] # 14-P2/4
  # - [-1, RepNCSPELAN4, [256, 512, 64, 1, False, 'silu']] # 15-P2/4

  # - [-1, SCDown, [256, 3, 2, 'silu']] # 16-P3/8
  # - [[-1, 12], Concat, []] # 17-P3/8
  # - [-1, RepNCSPELAN4, [256, 512, 64, 1, False, 'silu']] # 18-P3/8

  - [-1, SCDown, [256, 3, 2, 'silu']] # 12-P4/16
  - [[-1, 8], Concat, []] # 13-P4/16
  - [-1, RepNCSPELAN4, [256, 512, 64, 1, False, 'silu']] # 14-P4/16

decoder:
  - [[11, 14], DFINETransformer, {"feat_strides":[8, 16], "hidden_dim": 256, "num_levels": 2, "num_layers": 3, "num_points": [3, 6], "dim_feedforward": 1024}]