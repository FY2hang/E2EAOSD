backbone:
  # [from, module, args]
  # - [-1, StemBlock, [16, 16, True]] # 0-P2/4
  # - [-1, HG_Stage, [16, 64, 1, 3, False, False, 3, True, 'se']] # 1-P2/4
  # - [-1, HG_Stage, [32, 256, 1, 3, True, False, 3, True, 'se']] # 2-P3/8
  # - [-1, HG_Stage, [64, 512, 2, 3, True, True, 5, True, 'se']]  # 3-P4/16
  # - [-1, HG_Stage, [64, 512, 1, 3, False, True, 5, True, 'se']] # 4-P4/16
  - [-1, ConvNormLayer_fuse, [16, 3, 2]] # 0-P1/2
  - [-1, ConvNormLayer_fuse, [32, 3, 2]] # 1-P2/4
  - [-1, PFE_Net, [64, 2]] # 2-P2/4
  - [-1, ConvNormLayer_fuse, [64, 3, 2]] # 3-P3/8
  - [-1, PFE_Net, [128, 2]] # 4-P3/8
  - [-1, ConvNormLayer_fuse, [256, 3, 2]] # 5-P4/16
  - [-1, PFE_Net, [256, 2]] # 6-P4/16
  - [-1, ConvNormLayer_fuse, [512, 1, 1]] # 7-P4/16
  - [-1, PFE_Net, [256, 2]] # 8-P4/16

# 640x640x3
# 320x320x64

encoder:
  - [2, ConvNormLayer_fuse, [128, 1, 1]] # 9-P2/4
  - [4, ConvNormLayer_fuse, [128, 1, 1]] # 10-P3/8
  - [8, ConvNormLayer_fuse, [256, 1, 1]] # 11-P4/16

  # - [-1, TransformerEncoderBlock, [8, 1024, 0.1, "relu", 10000, False]] # 12-P4/16
  # - [-1, MetaFormer_Mona, [256, {'module': AdaptiveSparseSA, 'param':{'num_heads': 8, 'sparseAtt': True}}, {'module': ConvolutionalGLU, 'param': {'act_layer': nn.GELU, 'drop': 0.0}}, {'selfatt': True}]] # 7-P5/32
  - [-1, MetaFormer_Mona, [128, {'module': DPB_Attention, 'param':{'num_heads': 8}}, {'module': ConvolutionalGLU, 'param': {'act_layer': nn.GELU, 'drop': 0.0}}, {'selfatt': True}]] # 7-P5/32

  - [-1, ConvNormLayer_fuse, [128, 1, 1]] # 13
  - [-1, nn.Upsample, [None, 2, "nearest"]] # 14
  - [[-1, 10], Concat, []] # 15-P3/8
  - [-1, RepNCSPELAN4, [128, 256, 21, 2, False, 'silu']] # 16-P3/8

  - [-1, ConvNormLayer_fuse, [128, 1, 1]] # 17-P3/8
  - [-1, nn.Upsample, [None, 2, "nearest"]] # 18-P3/8
  - [[-1, 9], Concat, []] # 19-P2/4
  - [-1, RepNCSPELAN4, [128, 128, 21, 2, False, 'silu']] # 20-P2/4

  - [-1, SCDown, [128, 3, 2, 'silu']] # 21-P3/8
  - [[-1, 17], Concat, []] # 22-P3/8
  - [-1, RepNCSPELAN4, [128, 256, 21, 2, False, 'silu']] # 23-P3/8

  - [-1, SCDown, [256, 3, 2, 'silu']] # 24-P4/16
  - [[-1, 13], Concat, []] # 25-P4/16
  - [-1, RepNCSPELAN4, [256, 512, 64, 1, False, 'silu']] # 26-P4/16

decoder:
  - [[23, 26], DFINETransformer, {"feat_strides":[8, 16], "hidden_dim": 256, "num_levels": 2, "num_layers": 3, "num_points": [6, 6], "dim_feedforward": 1024}]