backbone:
  # [from, module, args]
  - [-1, StemBlock, [16, 16, True]] # 0-P2/4
  - [-1, HG_Stage, [16, 64, 1, 3, False, False, 3, True, 'se']] # 1-P2/4
  - [-1, HG_Stage, [32, 128, 1, 3, True, False, 3, True, 'se']] # 2-P3/8
  - [-1, HG_Stage, [64, 384, 2, 3, True, True, 5, True, 'se']]  # 3-P4/16
  - [-1, HG_Stage, [64, 384, 1, 3, False, True, 5, True, 'se']]  # 4-P4/16

encoder:
  # - [1, ConvNormLayer_fuse, [128, 1, 1]] # 5-P2/4
  - [2, ConvNormLayer_fuse, [128, 1, 1]] # 6-P3/8
  - [4, ConvNormLayer_fuse, [128, 1, 1]] # 7-P4/16

  - [-1, MetaFormer_Mona, [128, {'module': AdaptiveSparseSA, 'param':{'num_heads': 8, 'sparseAtt': True}}, {'module': ConvolutionalGLU, 'param': {'act_layer': nn.GELU, 'drop': 0.0}}, {'selfatt': True}]] # 7-P5/32
  # - [-1, NCHW2NLC2NCHW, [{'module': PolaLinearAttention, 'param':{'num_heads': 8, 'hw': [40, 40]}}]] # 7-P5/32
  # - [-1, MetaFormer_Mona_WithPola, [128, {'hw': [40, 40], 'num_heads': 8}, {'module': ConvolutionalGLU, 'param': {'act_layer': nn.GELU, 'drop': 0.0}}]]

  - [-1, ConvNormLayer_fuse, [128, 1, 1]] # 8
  # - [-1, nn.Upsample, [None, 2, "nearest"]] # 10
  # - [[-1, 5], Concat, []] # 11-P3
  - [[5, -1], SCEF, [128]] # 9
  - [-1, RepNCSPELAN4, [128, 64, 21, 2, False, 'silu']] # 10-P3/8
  # - [-1, Faster_Block, [64]]

  - [-1, SCDown, [128, 3, 2, 'silu']] # 11-P4
  - [[-1, 7], Concat, []] # 12-P4
  - [-1, RepNCSPELAN4, [128, 128, 21, 2, False, 'silu']] # 13-P4
  # - [-1, Faster_Block, [128]]

decoder:
  - [[10, 13], DFINETransformer, {"feat_strides":[8,16], "hidden_dim": 128, "num_levels": 2, "num_layers": 3, "num_points": [6,6], "dim_feedforward": 512}]