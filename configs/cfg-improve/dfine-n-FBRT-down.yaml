backbone:
  # [from, module, args]
  - [-1, StemBlock, [16, 16, True]] # 0-P2/4
  - [-1, HG_Stage, [16, 64, 1, 3, False, False, 3, True, 'se']] # 1-P2/4
  - [-1, HG_Stage, [32, 128, 1, 3, True, False, 3, True, 'se']] # 2-P3/8
  - [-1, HG_Stage, [64, 384, 2, 3, True, True, 5, True, 'se']]  # 3-P4/16
  - [-1, HG_Stage, [64, 384, 1, 3, False, True, 5, True, 'se']]  # 4-P4/16

encoder:
  # - [1, ConvNormLayer_fuse, [128, 1, 1]] # 5-P2/4
  - [2, ConvNormLayer_fuse, [128, 1, 1]] # 5-P3/8
  - [4, ConvNormLayer_fuse, [128, 1, 1]] # 6-P4/16

  - [-1, TransformerEncoderBlock, [8, 512, 0.1, "relu", 10000, False]] # 7*P4/16

  - [-1, ConvNormLayer_fuse, [128, 1, 1]] # 8
  # - [-1, nn.Upsample, [None, 2, "nearest"]] # 10
  # - [[-1, 5], Concat, []] # 11-P3
  - [[5, -1], SEEFusion, [128]] # 9
  - [-1, RepNCSPELAN4, [128, 64, 21, 2, False, 'silu']] # 10-P3/8
  # - [-1, Faster_Block, [64]]

  # - [-1, SCDown, [128, 3, 2, 'silu']] # 13-P4
  # - [[-1, 7], Concat, []] # 14-P4
  - [[8, -1], SEEFusion_Down, [128]] # 11
  - [-1, RepNCSPELAN4, [128, 128, 21, 2, False, 'silu']] # 12-P4
  # - [-1, Faster_Block, [128]]

decoder:
  - [[10, 12], DFINETransformer, {"feat_strides":[8,16], "hidden_dim": 128, "num_levels": 2, "num_layers": 3, "num_points": [6,6], "dim_feedforward": 512}]

